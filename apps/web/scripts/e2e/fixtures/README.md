# SDD019 browser fixtures

These fixtures define only role labels and viewport contracts. Test credentials
must be entered interactively or supplied through ignored environment variables;
do not commit passwords, tokens, sessions, or service keys here.

Use the role labels with disposable test data only. The fixture names do not
authorize access and must never replace normal authenticated browser flows.
