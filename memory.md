# Plotify Operational Memory

## Contrato con AGENTS.md

Lee [AGENTS.md](AGENTS.md) antes de usar esta memoria. Resume estado entre sesiones; no sustituye código, SDD ni el vault. Las reglas permanentes viven en AGENTS; el conocimiento profundo en `plotify_memori/`.

## Estado actual

- Verificado: 2026-08-22 — **Flujo de despliegue e infraestructura (Coolify self-hosted)**: El flujo de despliegue consiste en push a GitHub (`main`), el cual dispara el despliegue automático en Coolify alojado en un servidor local (on-premise) expuesto a través de túnel (Cloudflare Tunnel) y dominio propio. La arquitectura de ejecución corre en contenedores Docker (`docker-compose.yml` / `Dockerfile` para `web`, `api`, `worker` y `redis`), con persistencia en Supabase Cloud (`swkrnjdpnlrgxgotmfxy`). Fuente: decisión y configuración del usuario.
- Verificado: 2026-08-20 — **Fix carga de mapas en Telegram Mini App (/mini/mapa)**: Se diagnosticó y resolvió la causa por la que los mapas no cargaban en la Mini App. Causas raíz identificadas: (1) **Condición de carrera de ciclo de vida en React**: `MapaContent` (`page.tsx`) desmontaba completamente el DOM del mapa mientras `loading` o `sessionLoading` eran `true` (`if (sessionLoading || loading) return <Spinner />`), provocando que el `useEffect` de inicialización del mapa (`[]` deps) se ejecutara con `mapContainerRef.current = null` y se descartara permanentemente; (2) **Sincronización reactiva de MapLibre GL**: Se introdujo `mapInstance` en el estado y se vinculó `applyLayers` al evento `styledata` evitando bloqueos por esperar el evento único `load` si el estilo aún estaba procesándose; (3) **CSP de fuentes/glifos**: Se agregó `https://demotiles.maplibre.org` a la directiva `connect-src` en `content-security-policy.ts` para permitir la descarga de glifos de fuentes (.pbf) requeridas por las etiquetas de lotes. Gates: 121 suites vitest pasadas (957 tests verdes, 29 focales verdes), `pnpm typecheck:web` y pytest limpios.
- Verificado: 2026-08-20 — **Fix tormentas de consultas Realtime y CPU 100% en Supabase**: Se diagnosticó y resolvió la causa de la saturación de CPU en Postgres tras el SDD-021. Los paneles del dashboard (`vendor-requests-panel.tsx` y `pending-approvals-panel.tsx`) escuchaban `postgres_changes` sin limitador de frecuencia, disparando decenas de consultas simultáneas ante eventos CDC (ej. `read-all` o actualizaciones consecutivas). Fix: (1) Agregado control de throttle estricto de 15 segundos (`useRef`, `FETCH_THROTTLE_MS = 15_000`) a ambos paneles; (2) `notification-bell.tsx` sincronizado para actualizar `lastFetchRef` tras fetch exitoso amortiguando ecos inmediatos de Realtime. Verificado en Supabase Cloud (`swkrnjdpnlrgxgotmfxy`): 0 consultas activas bloqueando CPU, 13 conexiones basales, cache hit ratio >99.99%. Gates: 121 archivos vitest pasados (957 tests verdes), `pnpm typecheck:web` y `pnpm build:web` 100% limpios. Commit `b80282f` pusheado a `main`. Fuente: Supabase MCP + inspección pg_stat_activity + gates.
- Verificado: 2026-08-18 — **Rediseño Integral PropTech de la Telegram Mini App (/mini)**: Se completó la reestructuración y rediseño visual de todas las vistas de la Mini App para Vendedor y Administrador bajo los estándares de diseño y paleta oscura de Plotify (`#121212`, `#161616`, `#181818`, acentos `#16A34A` y `#c24444`). (1) **Navegación Móvil Ergonómica**: Implementada la Bottom Navigation Bar adaptativa (`mini-app-nav.tsx`) y cabecera unificada (`mini-app-header.tsx`) con soporte nativo de Telegram `HapticFeedback` (`impact`, `notification`, `selection`). (2) **Vista Vendedor Comercial y Limpia**: Eliminado el lenguaje técnico y alarmista ("Bloqueadas", "Blockers", "Fallo de cascada") en `/mini/ventas` y `/mini/ventas/[caseId]`, reemplazado por un pipeline de avance de 4 fases comerciales (*Ingresada*, *En Validación*, *Revisión Legal*, *Minuta Lista*), acceso rápido al Masterplan y botón para crear reserva. (3) **Centro de Mando Ejecutivo para el Administrador**: `/mini/bandeja` transformado de bandeja genérica a centro de control de decisiones con KPIs en tiempo real (Aprobaciones vs Excepciones) y acceso directo al visor satelital; `/mini/bandeja/[id]` modernizado con fichas de decisión y resolución de discrepancias legales. (4) **Visor Satelital y Formulario de Reserva**: `/mini/mapa` equipado con selector de proyectos y contadores de disponibilidad en vivo; `/mini/reserva` ahora permite tanto selección autónoma de proyecto/lote disponible como preselección desde el mapa, con validación de RUT y `MainButton` de Telegram. Gates: 121 archivos vitest web pasados (957 tests verdes), `pnpm typecheck:web` y `pnpm build:web` 100% limpios con Turbopack, `check:agent-context` verde. Fuente: solicitud del usuario + UX/UI Plotify + gates.
- Verificado: 2026-08-18 — **Skills del proyecto trackeadas (105/112 con upstream + 7 locales)**: se resolvieron upstreams vía skills.sh y reinstalación con `npx skills add`. Orígenes: `wshobson/agents` (55: python-*, api-design-principles, langchain-architecture, track-management, workflow-*, etc.), `nextlevelbuilder/ui-ux-pro-max-skill` (ui-ux-pro-max, banner-design, brand, design, slides — las ckm-* locales custom se reemplazaron por upstream por decisión del usuario), `dceoy/speckit-agent-skills` (9 speckit), `vercel-labs/openreview` (3 next-*), `vercel-labs/agent-skills` (vercel-composition-patterns, vercel-react-best-practices), `vercel/next.js` (2 next-cache-components-*), y el resto de la sesión previa (addyosmani, anthropics, supabase, etc.). Sin tracking (locales/custom): `plotify-sdd-handoff`, `telegram-integration`, `speckit-git-commit/feature/initialize/remote/validate` (spec-kit publica commands, no SKILL.md; no hay fuente válida en skills.sh). `npx skills update -y` final: 105 actualizadas, sin skills sin tracking. Fuente: skills.sh API + CLI skills + skills-lock.json verificado.
- Verificado: 2026-08-18 — **Fix `Failed to fetch` en `TelegramBotSetup` (Dashboard)**: el componente cliente (`telegram-bot-setup.tsx:57,90,112`) tenía URLs hardcodeadas `http://localhost:8005/api/v1/...` provocando error de CORS/origen cruzado en el navegador (`Failed to fetch`) → `fetchStatus` fallaba en silencio dejando `status=null` y mostrando siempre el formulario vacío "Crear/Conectar Bot" aunque el bot ya estuviera creado en BD. Corregido a rutas relativas `/api/v1/bots/...` que Next.js reenvía vía rewrite a `:8005`. Verificado: `curl http://localhost:3000/api/v1/bots/<org_id>` retorna `{"bot_username":"plotify_chat_bot","is_active":true}` y el componente ya renderiza la tarjeta verde "Bot Configurado (@plotify_chat_bot)". Gates: typecheck y vitest verdes.
- Verificado: 2026-08-18 — **Fix hydration mismatch del SDK de Telegram en /mini**: `next/script` (client component) re-renderiza el `<script>` al hidratar y el navegador oculta el atributo `nonce` del DOM cuando hay CSP con nonce (bug Chromium 1211471) → React difeaba `nonce=""` (DOM) vs el nonce real y reportaba mismatch. Fix: `<script src="https://telegram.org/js/telegram-web-app.js" nonce={nonce}>` plano en `mini/layout.tsx` (server component, render-blocking: el SDK sigue cargando antes de hidratar; sin representación client-side no hay diff). Test `miniapp-sdk-script.test.ts` actualizado (3 tests: SDK antes de hidratar, script plano sin next/script, shell directo). Gates: vitest focal 23 passed, typecheck/build verdes, HTML servido verificado con script+nonce. Fuente: Context7 (next/script nonce handling + isEqualNode de head-manager) + verificación del HTML dev.
- Verificado: 2026-08-18 — **Fix lote de bugs funcionales de la miniapp de Telegram** (sin SDD formal, decisión del usuario: bugs primero, rediseño visual después). (1) **Mapa roto en 3 capas**: `mini/mapa/page.tsx` consultaba `/api/projects` (endpoint CRM con auth por cookies de Supabase, inútil dentro de Telegram → 401) y esperaba un array con `name` cuando el endpoint devuelve `{projects, role}` con `nombre` → nuevo endpoint API `GET /api/v1/miniapp/proyectos` (vendedor → proyectos de `vendor_projects`; admin → todos los de la org) + proxy Next `/api/miniapp/proyectos` + página actualizada. (2) **Detalle de bandeja con contrato roto**: `bandeja/[id]/page.tsx` reescrito al contrato real `BandejaDetail` (`detalles_lote{numero,precio,proyecto}`, `comprador{nombre,rut,email,telefono}`, `conflictos[]{nombre,valor_certificado,valor_vendedor,diferencia_detectada}`, `evidence_file_id` opaco sin URLs de storage); helper compartido `lib/miniapp/format.ts` (formatRelativeTime/formatCLP). (3) **Deep link `startapp=lot_<id>` roto**: `resolveMiniAppOrgId()` en `lib/miniapp/routes.ts` ignora `start_param` con prefijo `lot_` como org (usaba el lote como org_id → 403) y el redirect a `/mini/mapa` ahora conserva `org` vía `miniAppUrl`. (4) **Compartir lote usaba bot hardcodeado** `PlotifyBot` (el real en BD es `plotify_chat_bot`): `MiniappSessionResponse` ahora incluye `bot_username` (desde `telegram_bots`), persiste en la sesión web y `lot-sheet.tsx` arma `t.me/<bot_username>/app?startapp=lot_<id>` con feedback de error si no hay bot. (5) Guard `blockers_humanizados?.[0]` en ventas. Tests nuevos: 3 pytest endpoint proyectos + 1 pytest bot_username + 3 guards vitest (mapa-endpoint, bandeja-detail-contract, share-blockers) + casos en deep-links. Gates: pytest 988 passed/1 fail preexistente (`test_trigger_exists_in_db`), vitest web 956 passed, typecheck/build/lint sin hallazgos nuevos, format sin warns nuevos (16 preexistentes; el JSON del contrato ya fallaba prettier en HEAD), contrato OpenAPI regenerado (37 líneas: /proyectos + bot_username). **API corre en :8005** (no 8000) y está viva: uvicorn recargó el código (el endpoint nuevo `/api/v1/miniapp/proyectos` responde en vivo; proxy Next en :3000 responde 401 sin token). Pendiente: rediseño visual de pantallas (postergado por decisión del usuario). Fuente: TDD (RED confirmado en los 6 fixes) + CodeGraph + Supabase MCP + gates.
- Verificado: 2026-08-18 — **Dedupe idempotente de notification_events (FR-006, SDD 021)**: índice único `idx_notification_events_unique_approval_recipient (approval_id, recipient_id)` (migración `20260818000000`) aplicado a cloud con 0 duplicados preexistentes; los 2 productores insertan con `upsert(ignore_duplicates=True)` → `INSERT ... ON CONFLICT DO NOTHING` (`approval_notifier.py`, `approval_processor.py`); fake del hook con semántica DO NOTHING. Tests nuevos: `test_notifications_dedupe.py` (3, incluye 2 de concurrencia con `asyncio.gather` que reproducían el bug: 2 filas → 1), 4 cross-tenant reales en `test_notifications_management.py` (list/dismiss/read/read-all con miembro de org B → 403) y 1 vitest FR-005 (bell no crashea ante 403). Suite API 984 passed/1 fail preexistente; vitest focal 26 passed; `verify:migrations` y `check:agent-context` verdes; lint web sin errores nuevos (6 preexistentes `no-explicit-any`). Fuente: TDD (RED confirmado) + Context7 (postgrest-py 0.17 upsert) + Supabase MCP + gates.
- Verificado: 2026-08-17 (SDD 021 revisado como senior y corregido: format tasks.md/feature.json, ítem focusable por teclado US7, dismiss admin alineado a `recipient_role=admin`; evidencia `specs/021.../evidence/openapi-diff-notes.md` documenta deuda arrastrada del contrato; gates re-ejecutados — pytest 61 passed/1 fail preexistente, vitest 25 passed, typecheck/build/verify:migrations verdes; commits `fa2df82` (fix replay/scope) y `4c97e32` (SDD 021); handoff en `plotify_memori/50 - Implementaciones/SDD 021 Notificaciones Gestion Completa - Handoff.md` y Home actualizado). Suite API 971 passed / 4 fails preexistentes (14-08) sigue como referencia; smoke live con 2 filas reales `dismissed_at` en BD.
- Feature SDD activa: `specs/019-hardening-produccion/` (reabierta tras cerrar 021; pendiente T079–T082 outbox durable). **SDD 021 cerrado del todo el 2026-08-18**: rama `021-notificaciones-gestion-completa` mergeada a `main` (fast-forward hasta `321c59c`, 11 commits incluyendo dedupe FR-006 y batching N+1). El proceso nohup de la API usado para el smoke previo fue eliminado — `pnpm dev:api` queda a cargo del usuario.
- Worker ARQ reiniciado con código nuevo (verificado: start 17:09 local > mtime 12:49 de database.py; API 14:19 también posterior).
- F4 verificado: 0 zombies reales (>5min), outbox 12/12 completed, worker procesa ciclos sin acumular transacciones huérfanas.
- CodeGraph: sano y sincronizado.
- Camino corto A1–A5: `core/config.py` (hard-off false), `escritura_auto_pipeline.py` (rollout desde RPC SQL + gates relajables), migración `20260807235200_sdd019_relaxed_readiness.sql`.
- F1–F6 pool PostgREST: singleton cliente en `core/database.py` (timeout 30s, pool limitado), migraciones `20260812165003_sdd019_idle_transaction_timeout` y `20260812165010_sdd019_outbox_lock_timeout` aplicadas al linked (parity 0/0), script `apps/api/scripts/check_idle_transactions.py`, runbook `docs/runbooks/postgres-connection-pool.md`, test de regresión singleton.

## Fuentes

- Reglas: [AGENTS.md](AGENTS.md).
- Principios: `.specify/memory/constitution.md`.
- SDD activo: `specs/019-hardening-produccion/`.
- Código y dependencias: manifiestos, lockfile, migraciones y tests.
- Vault Obsidian: `plotify_memori/`; no usar `.obsidian/` como fuente de producto.
- [Mapa Técnico Actual](plotify_memori/30%20-%20Arquitectura/Mapa%20T%C3%A9cnico%20Actual.md).

## Stack técnico verificado

Fuente: `package.json`, `apps/web/package.json`, `apps/api/requirements.txt`, `pnpm-lock.yaml` y runtimes locales; verificado 2026-08-06.

- Node: mínimo `>=22.13`; runtime observado `26.5.0`.
- pnpm: `11.22.0` (brew; actualizado 2026-08-18 desde 11.3.0).
- Web: Next `16.2.6`, React `19.2.8`, TypeScript 5, Tailwind 4, shadcn `4.8.0`, Vitest `4.1.10`, Playwright `1.61.1`.
- API/worker: Python `3.13.13`, FastAPI `0.135.1`, LangGraph `1.2.1`, ARQ `0.27.0`. La API dev corre en `:8005` (PLOTIFY_CHAT_BASE_URL del web apunta ahí; `:8000` no es el puerto del servicio).
- Datos: Supabase cloud-only `swkrnjdpnlrgxgotmfxy`. PostgreSQL 17 está declarado por SDD019 y requiere confirmación Supabase MCP antes de afirmar estado del servicio.
- Despliegue / Infraestructura: GitHub (push a `main`) → Coolify en servidor local on-premise/self-hosted con túnel y dominio propio; orquestación con Docker / `docker-compose.yml` (`web` :3000, `api` :8005, `worker` ARQ, `redis` :6379).

## Correcciones

### Pendientes de verificar

- 2026-08-18 — Update de dependencias: pnpm 11.3.0→11.22.0 (brew), `pnpm update -r` (sin majors) + react/react-dom 19.2.8 (pinned exacto, subido manual). Gates post-update: typecheck/build web ✓, vitest 944/944 ✓, check:agent-context ✓, lint con 6 errores preexistentes en `notification-components.test.tsx` (SDD 021, `no-explicit-any`), format:check con 14 warns preexistentes. `variable-matrix.test.tsx` estaba roto en HEAD desde `644e983` (03-ago: lógica de approve condicional por status sin actualizar mocks) — verificado en worktree con pnpm 11.3.0; corregido alineando mocks al API real (submit→`legal_review_pending`). Fuente: verificación con worktree + vitest.

- 2026-08-07 — Secuencia exacta de excepciones por intento del consumidor outbox (sospecha: `WORKFLOW_LEASE_LOST` al llamar `complete()` tras el defer de la cascada). Falta confirmar con logs del worker ARQ; el usuario no adjuntó las terminales. Fuente: diagnóstico en vivo.
- 2026-08-07 — Reconciliación de migraciones EJECUTADA y verificada (A3b completo): Fase 1 registró en `schema_migrations` las 5 versiones locales LLM + `20260806000000` (statements=ARRAY[], DDL ya aplicado) y retiró las 5 remote-only LLM (metadata, DDL intacto). Fase 2.1 creó `organizations.escritura_relaxed_readiness`; Fase 2.2 re-aplicó `is_project_vendible` con el flag relajado. Registrada también `20260807235200`. Resultado: `supabase migration list --linked` 45/45 `local==remote`; `compare-migration-history --target linked` → `exact_parity`. Backup lógico en `specs/019-hardening-produccion/evidence/migration-reconciliation-backup.json`. Fuente: ejecución directa + verificación.
- 2026-08-07 — Fix en `packages/database/scripts/compare-migration-history.mjs`: (1) `inspectLinkedMigrationHistory` corría `supabase migration list` desde la raíz del monorepo (nunca veía locales) → corregido cwd a `packages/database/supabase`; (2) `parseRemoteMigrationList` esperaba salida tabular, pero la CLI devuelve JSON → soporta JSON con fallback legacy. 2 tests nuevos: 6/6 verdes. Este bug invalidaba el gate de parity del SDD019. Fuente: verificación.

### Camino corto (A1–A5) — implementado y verificado

- 2026-08-07 — Implementado el "camino corto" para generar escrituras: `PLOTIFY_HARD_OFF_*` default `false` (`core/config.py`); `run_case_cascade` resuelve el rollout desde la RPC SQL `resolve_feature_rollout` (fuente de verdad) en vez de `control=None` (elimina `AUTOMATIC_ESCRITURA_MISSING`); nuevo flag de org `escritura_relaxed_readiness` (migración `20260807235200_sdd019_relaxed_readiness.sql`) que relaja los gates heredados `title_verified`/`sii_verified`/`sag_plano_verified` en la cascada. `is_project_vendible` (migración `20260806000000`) ahora respeta el flag: con él basta matriz aprobada para vender. Four-eyes ya era `false` por defecto. Evidencia: 946 tests pytest verdes (1 live fail preexistente `test_notifications_fase7::test_trigger_exists_in_db` por función `query` ausente en BD). Fuente: implementación + verificación.
- 2026-08-07 — Tests de API rotos por el cambio manual del usuario `maybe_single()→limit(1)` + consulta separada a `projects` en `_assert_lot_scope`: se actualizaron los mocks de `test_pipeline_venta_escritura_contract.py`, `test_escrituras_readiness.py`, `test_matriz_operational_bridge.py` y `test_matriz_operational_gates.py` para soportar la tabla `projects` y el método `.limit()`. Fuente: verificación de suite.

- 2026-08-17 — **Fix bugs cascada de escritura, worker outbox y 500 en variables de proyecto (Lote 38)**. Aislamiento por capas de 3 bugs encadenados:
  1. **API/Frontend 500 en variables**: En `legal_variable_resolution.py:1317`, `str(row_lot) or ""` con `row_lot=None` evaluaba a `"None"`, por lo que `by_key_scope.get((key, ""))` nunca encontraba las variables de proyecto existentes e intentaba reinsertarlas provocando `23505 duplicate key variable_resolutions_active_scope_idx` y error 500 en `GET /api/v1/legal-variables/project/:id`. Corregido a `str(row_lot) if row_lot else ""`.
  2. **Worker Outbox en Dead Letter**: En `escritura_workflow_outbox.py`, la clase `WorkflowOutboxItem` con `slots=True` no tenía el campo `event_type`. Al evaluar `attempted.event_type` en `workers/tasks/escritura_workflow_outbox.py:101`, Python lanzaba `AttributeError`, atrapado como `WORKFLOW_TRANSIENT_FAILURE` y enviando todas las ventas a `dead_letter`. Corregido agregando `event_type: str = "sale_approved"`.
  3. **Cascada abortando por semántica en gates relajados**: En `escritura_auto_pipeline.py:751`, tras `_system_approve_legal_review`, el refresco del snapshot con `create_escritura_case_snapshot` descartaba los placeholders en memoria de `_inject_relaxed_placeholders`. `_system_approve_matriz` intentaba validar el DOCX sin el valor `(pendiente)` para `lote.rol_tramite` y fallaba con `DOCUMENT_SEMANTIC_INVALID` (outcome=exception). Corregido reinyectando los placeholders relajados tras refrescar `case_row`.
  - Tests: 88 passed en suite focalizada (2 tests de regresión nuevos agregados), suite API 978 passed / 1 fail preexistente (`test_trigger_exists_in_db`). Fuente: Supabase MCP + Sequential Thinking + Pytest.

- 2026-08-17 — **Optimización de intervalos de cron en ARQ worker**. Ajustados los cron jobs de fallback en `main_worker.py` para eliminar el sondeo innecesario a la base de datos durante tiempos de inactividad: (1) `process_escritura_workflow_outbox` espaciado de 1 min a cada 15 min (`minute={0,15,30,45}`); (2) `reconcile_legal_document_ingestions` de 2 min a cada 30 min (`minute={0,30}`); (3) `check_idle_transactions_cron` de 10 min a cada 30 min (`minute={15,45}`). Los flujos primarios continúan ejecutándose en tiempo real vía eventos inmediatos (`enqueue_job`). Gates: pytest 12 passed, agent-context limpio. Fuente: CodeGraph + Pytest.
- 2026-08-17 — **Hardening anti-zombies y timeouts en `service_role`**. Hallazgo: 3 conexiones PostgREST zombi (PIDs 86558, 87269, 98907) llevaban 3.8 días activas llamando `begin_workflow_outbox_attempt` (generando 2.500 req/s y 99% CPU en Supabase) porque `service_role` tenía `rolconfig=null` (sin timeouts), a diferencia de `anon`/`authenticated`. Acciones ejecutadas: (1) Terminados los 3 backends zombi con `pg_terminate_backend`; (2) Migración `20260817120000_service_role_transaction_timeouts.sql` creada y aplicada a Supabase Cloud con `idle_in_transaction_session_timeout='30s'`, `statement_timeout='60s'` y `lock_timeout='10s'` sobre `service_role`; (3) Worker `idle_transaction_monitor.py` actualizado a `terminate=True` con `max_age_seconds=120` para auto-purgar zombis cada 10 min. Gates: pytest 40 passed, assert-canonical-migrations ok, agent-context limpio. Fuente: Supabase MCP + CodeGraph + Pytest.
- 2026-08-17 — **Optimización de consumo BD y UI en SDD 021**. (1) **Front UI**: Ícono de descarte cambiado a `Delete02Icon` (papelera) con `title="Eliminar notificación"` y reubicado dentro del header flex junto al Badge (elimina solapamiento absoluto de layout con badges largos); (2) **API Batching**: Eliminado el patrón N+1 de consultas en `list_notifications` pre-cargando `escritura_cases` y `escritura_matrices` en 2 consultas batch (`.in_()`), reduciendo hasta 95% de tráfico PostgREST por listado; (3) **DB Índices**: Migración `20260817110000_sdd021_notification_events_active_indexes.sql` aplicada a Supabase Cloud con índices parciales `idx_notification_events_active_admin` y `idx_notification_events_active_recipient` (`WHERE dismissed_at IS NULL`). Gates: pytest 22 passed, vitest 25 passed, tsc limpio. Fuente: Supabase MCP + Pytest + Vitest + CodeGraph.
- 2026-08-17 — **Revisión senior del SDD 021 y correcciones aplicadas**. Hallazgos: (1) `format:check` introducía 2 warns (`specs/021/.../tasks.md`, `.specify/feature.json`) → formateados; (2) US7: el ítem no era focusable por teclado → `tabIndex` + Enter/Espacio en `notification-item.tsx` + 2 tests; (3) dismiss de admin era org-wide (inconsistente con list/read-all) → ahora exige `recipient_role=admin` + test `test_dismiss_admin_cannot_dismiss_vendor_notification`; (4) el diff del OpenAPI regenerado arrastraba el endpoint `ensure_legal_document_ingestion` (deuda previa de regeneración, JSON de HEAD desactualizado) → aceptado y documentado en `evidence/openapi-diff-notes.md`; (5) el fix de duplicados del 14-08 NO estaba commiteado (working tree mezclado) → separado en commit `fa2df82`. Verificado en código: `update()` de postgrest-py 0.17 devuelve `return=representation` por defecto → `updated_count` de read-all es correcto en producción. Gates: pytest 61 passed/1 fail preexistente (`test_trigger_exists_in_db`), vitest 25 passed, typecheck/build/verify:migrations verdes. Commits: `fa2df82`, `4c97e32`. Fuente: revisión de código + Supabase MCP + ejecución de gates.
- 2026-08-14 — **Fix flujo de notificaciones (2 bugs raíz) en `main`**. (1) **Duplicados al vendedor**: el webhook Telegram (`webhook.py:306-322`) ejecuta la decisión inline (`_run_decision_and_cascade`) Y encola `process_admin_decision`; como `approve_sale` responde `success=true, replayed=true` en replay (migración `20260812225000`), la 2ª ejecución no fallaba con 409 → volvía a correr hook/cascada/auditoría y `send_decision_notifications` duplicaba el mensaje de Telegram al vendedor y las filas `notification_events`. Evidencia BD (venta cfd19374): 3× `sale.approved` en audit_logs (1 RPC + 2 Python) y 2 eventos vendor; patrón en las últimas 4 ventas. Fix: `execute_admin_decision_db` retorna temprano con `replayed=True` sin hook/cascada/audit; `send_decision_notifications` y `process_admin_decision` retornan `REPLAYED` sin enviar. (2) **Campana web del vendedor mostraba notificaciones del admin (FR-006)**: `notifications.py` filtraba por `user_role == "vendor"`, rol inexistente en `organization_members` (solo `admin`|`user`; el modelo canónico está en `miniapp_session.py:269` — vendor = user + fila activa en `vendors`). El vendedor caía en la rama admin (veía pendientes de Matías y no las suyas) y en mark-read podía marcar notificaciones ajenas. Fix: `_resolve_member_notification_scope` (admin | vendor vía tabla vendors | user) aplicado a list y mark-read. Data-fix: 4 filas `notification_events` duplicadas eliminadas (manteniendo la más antigua). 9 tests de regresión nuevos; suite 957 passed / 4 fails preexistentes (3 `test_escrituras_variable_inventory` + 1 `test_trigger_exists_in_db`). API (PID 43091) y worker ARQ (PID 43143) reiniciados con código nuevo. Fuente: CodeGraph + Supabase MCP + pytest. Nota 2026-08-17: este fix no estaba commiteado; quedó en working tree y se commiteó en `fa2df82` sobre la rama 021.

- 2026-08-13 — **Fix bugs endémicos "falta aprobar" en venta/cascada de escritura (Lote 26 Teno 2) implementado en rama `fix/escritura-cascade-state`**. Cuatro bugs de persistenciaencadenados corregidos + data-fix retroactivo + 3 migraciones en BD linked. (A1) `_upsert_lot_variable` (`escritura_case_workflow.py:2442`) ahora acepta `actor: Literal['human','system']`; para `actor='system'` persiste `state='approved'` (no `resolved`) cuando siembra `revision_juridica.*` en cascada auto-approve. La auditoría sigue viviendo en `legal_review_decisions` (decided*by=NULL, origin='system', trigger). (A2) `_run_case_cascade` (`escritura_auto_pipeline.py:~737,~858`) ahora invoca `create_escritura_case_snapshot` tras `_system_approve_legal_review` (A2) y al final exitoso (A3 defensivo) — antes el snapshot `readiness_gates` quedaba congelado previo a la siembra del puente y Telegram/mesa reportaban "falta aprobar" pese a cascade `completed`. (C) `_system_approve_matriz` (`escritura_auto_pipeline.py:367`) eliminó la bifurcación `hasattr(client, "rpc")` que dejaba 2 caminos divergentes (real con RPC `begin/finalize_matriz_approval`+validación semántica vs legacy de tests que sólo UPDATEaba status='approved' sin verificar). Los fakes de tests now reflect production. (D) `_run_case_cascade` re-lee la matriz de BD tras `_system_approve_matriz`; si el RPC de aprobación dejó `status != 'approved'` (validación semántica del DOCX falló) la cascada queda en `outcome='exception'` con `kind='matriz_approval_failed'` (antes registraba `step approve=executed` false-positivo pese a status='draft').
  **(A1-bis+A6)** `escritura_operational_bridge.py:927` puente siembra comprador/lote/servidumbre/transaccion con `state='approved'` (si source_type=system/geometry) o `state='derived'` (si source_type=derived — precio_letras, superficie_texto, etc.) en vez del `state='resolved'` hardcodeado. La mesa del molde cuenta `resolved` como "por aprobar" → generaba ~31 "Falta" en el caso Lote 26 pese a tener valores ya sembrados.
  **(A4)** `_fetch_variable_resolution_rows` (`legal_variable_resolution.py:2072`) ahora mergeea filas scope proyecto + scope lote cuando se pide `lot_id`: `OR lot_id.eq.X,lot_id.is.null`. Antes excluía las del molde del proyecto (sag.*, vendedor.nombre, evidencia.certificado_gp_referencia, mandato.*) y el caso Lote 26 las mostraba como "Falta" aunque estuvieran aprovadas a nivel proyecto.
  **(A5)** `_ensure_authored_variable_gaps` extendido: (a) ahora corre también con `lot_id` no None (antes sólo en molde proyecto); (b) siembra authored con default del catálogo como `state='derived'` en vez de `missing` (mandato.facultades, cláusulas boilerplate); (c) NO siembra en scope lote si ya existe fila activa en scope proyecto con valor (no duplica authored por proyecto).
  Migraciones: `20260813220000_sdd019_relax_approved_review_check.sql` (v1) + `20260813221000_sdd019_relax_approved_review_check_v2.sql` + `20260813221500_sdd019_relax_approved_review_check_v3.sql` aplicadas al linked (parity 60/60). v3 afloja el `CHECK variable_resolutions_approved_review_check` para permitir `state='approved'` con `reviewed_by=NULL`/`reviewed_at=NULL` cuando `source_type IN ('legal_review','system','geometry','derived')` — auditoría de actos del sistema vive en `legal_review_decisions`/`audit_logs`/`approval_requests`, no en `reviewed*_`de`variable_resolutions`.
Data-fix retroactivo: (a) 41 filas `revision_juridica._` (`resolved`→`approved`); (b) ~20 filas puente `resolved`→`approved`/`derived`del Lote 26 + otros lotes con venta de Teno 2; (c) 5 filas authored`manual/resolved`→`approved`en molde Teno 2 (doc.abogado_redactor.nombre/rut, mandato.rectificacion_nombre/rut, mandato.facultades); (d) DELETE 2 filas`mandato.rectificacion\_\*`en scope-lote Lote 26 sembradas por erróneo por A5 (pisaban las del molde). Refresh de`escritura_cases.readiness_gates`de 12 lotes variables_pending de Teno 2.
**Verificación final**: inventario del caso Lote 26 con`lot_id=fc8d1627`— total=246 vars, 0 "por_revisar" (222 approved + 17 derived + 7 not_applicable); el snapshot del caso quedó`readiness_status=blocked`solo por`sii_verified` (`lote.rol_tramite`ausente en`lot_legal_data`— bloqueo legítimo confirmado por el usuario, no un bug). Test suite escritura: 209 passed / 1 skipped, 1 fail preexistente`test_trigger_exists_in_db`por función`query`ausente. Worker ARQ reiniciado limpio con código nuevo (PID posterior al commit).
La decisión`sii_verified`para Lote 26 (legítimamente bloqueado por falta de rol SII asignado al lote) queda fuera del alcance del hotfix — el usuario confirmó que es esperado. Pendiente post-fix: vigilar ventas nuevas en`exceptions_only` para confirmar que el snapshot stale ya no ocurre y que la mesa del molde muestra 0 pendientes excepto rol_tramite cuando no haya rol asignado.

**C-bis (extensión)**: `_run_case_cascade` también captura `HTTPException` con `DOCUMENT_SEMANTIC_INVALID` lanzado por `_approve_semantic_candidate` al renderizar el DOCX → mapea los `issues` a causes humanizadas y corta con `outcome='exception' kind='matriz_approval_failed'` (antes la excepción escapaba sin captura). Verificado: reintento manual del Lote 40 (que no tenía rol_tramite) → cascada quedó exception con kind=matriz_approval_failed y notificó al admin por Telegram (Chat ID 5844273174一个好的好). Sin el fix la cascada hubiera reportado completed con status=draft (bug original). Nuevo test `test_matrix_semantic_approval_failure_yields_exception` cubre este caso en `test_escritura_auto_pipeline.py::TestRealBlockersException`. Suite final escrituras: **210 passed / 1 skipped**, 1 fail preexistente `test_trigger_exists_in_db`.

**Revalidación Teno 2 completa**: se refresh el inventario (`_ensure_authored_variable_gaps`) y el snapshot del caso (`create_escritura_case_snapshot`) para los 13 lotes `variables_pending` del proyecto Teno 2. Resultado: **12/13 lotes quedan bloqueados SÓLO por `sii_verified`** (legítimo — lotes sin fila en `lot_legal_data`). Único outlier: **Lote 40** — bloqueado por `sii_verified` Y `legal_review_ready`. Investiggación: el caso del Lote 40 corrió la cascada 2 veces el 2026-08-13 con `outcome='exception' n_steps=0` (cortó por blockers reales en pré-seed del puente), por lo que `_system_approve_legal_review` nunca se ejecutó y `revision_juridica.*` nunca fue sembrada para ese lote — flujo legítimo, no bug. Un reintento limpio con el fix aplicado (post-refresh snapshot) confirma que el bug 4 se captura correctamente y la cascada queda exception con cause accionable sobre `lote.rol_tramite`. Fuente: implementación directa + Supabase MCP + pytest + worker relanzado + inventario API Python inline.

- 2026-08-13 — **Fix permanente carrera "superseded sin reemplazo" implementado (opción 1: upsert atómico)**: migraciones `20260813180000` + `20260813181000` crean el RPC `batch_upsert_variable_resolutions(jsonb)` (SECURITY DEFINER, search_path='', solo service_role/postgres): supersede + insert por fila en UNA transacción con `LOCK TABLE ... SHARE ROW EXCLUSIVE` y `SET CONSTRAINTS ALL DEFERRED`; v2 convirtió el FK `variable_resolutions_superseded_by_fkey` a `DEFERRABLE INITIALLY IMMEDIATE` (v1 fallaba 23503 al setear superseded_by antes de insertar). El RPC además enlaza `superseded_by` al nuevo id. `persist_proposals` (`legal_variable_resolution.py`) ahora llama el RPC (se eliminó el patrón PATCH-supersede + INSERT batch). Verificado en BD con transacciones rollback (insert → supersede+link → insert nuevo; 23503 resuelto). Suite pytest: 950 passed / 1 fail pre-existente `test_trigger_exists_in_db`. Fakes de tests actualizados (`test_matriz_operational_bridge`, `test_escrituras_variable_resolution`, `test_pipeline_venta_escritura_contract`, `test_escrituras_text_extraction`). Pendiente: reiniciar API y worker para cargar el código nuevo. Fuente: MCP + CLI linked + pytest.
- 2026-08-13 — **Bug "superseded sin reemplazo" confirmado en venta Lote 40 (Teno 2)**: el usuario aprobó con datos ya verificados (`lots.m2=5062.07`, `area_official_m2`, `verified_status='verified_exact'`, boundaries ✓), pero la cascada reportó 6 tokens faltantes (superficie_m2/texto/ha_texto, servidumbre.superficie_texto, servidumbre.deslindes_tramo, transaccion.precio_letras). Causa raíz: la aprobación ejecuta el staging del puente operacional DOS veces en paralelo (cascada inline del webhook API + job ARQ `process_admin_decision`); el segundo run supersedeó filas del primero y su INSERT falló con `23505 duplicate key variable_resolutions_active_scope_idx` → 6 filas quedaron `state='superseded'` SIN reemplazo con valores ya calculados correctamente (p. ej. `lote.superficie_m2='5062.07'`, precio_letras="dieciocho millones de pesos"). Data-fix aplicado vía MCP: `UPDATE ... SET state='resolved' WHERE extractor_name='operational_bridge_v1' AND state='superseded' AND value_text IS NOT NULL` (7 filas). El cron del outbox re-snapshotea el caso y los warnings desaparecen. Fix permanente implementado arriba (RPC atómico). Fuente: logs worker + pg + código del bridge.
- 2026-08-13 — **Bug aprobación de venta 23514 (fingerprint outbox)**: `20260812225000_sdd019_rpc_hash_optimization.sql` cambió `approve_sale` a `event_fingerprint = md5(...)` (32 hex) pero NO actualizó el CHECK `workflow_outbox_event_fingerprint_check` (seguía exigiendo 64 hex sha256) → toda aprobación de venta nueva fallaba con `23514: new row for relation "workflow_outbox" violates check constraint "workflow_outbox_event_fingerprint_check"`. Primera venta post-migración (lote 22, hoy) lo expuso; las 12 filas outbox previas eran sha256. Fix: migración `20260813170000_sdd019_fix_outbox_fingerprint_constraint.sql` → CHECK `^([0-9a-f]{32}|[0-9a-f]{64})$`, aplicada y verificada. No hay otras constraints afectadas por el md5 (audit_event_key no tiene regex; request_hash mantiene sha256 por decisión del plan). `approval_requests.a6f7517c` sigue `pending` (el RPC fallido hizo rollback) — reintentar approve. Fuente: logs worker/API + pg_constraint + MCP.
- 2026-08-13 — Diagnóstico y resolución "venta lote 22 (Teno 2) sin notificación Telegram": **la BD NO fue la causa**. La solicitud se persistió OK en BD (`approval_requests.a6f7517c`, 18:08 UTC, status `pending`, lot `ba477ee9` Lote 22 Teno 2). La causa raíz fue un **loop infinito de cálculo de cron en ARQ** dentro de `apps/api/workers/main_worker.py`: los cron jobs tenían configurado `second=120`, `second=60` y `second=600`. En `arq.cron`, `second` debe ser un segundo del minuto (0–59); un valor >= 60 provocaba que `_get_next_dt()` / `next_cron()` entrara en un bucle `while True` infinito sin yield/await evaluando fechas hasta el año 9999 al 100% de CPU durante el startup del worker, impidiendo que el worker consumiera `arq:queue`. Fix aplicado: se cambiaron los cron schedules a `minute=set(range(0, 60, 2)), second=0`, `minute=None, second=0` y `minute=set(range(0, 60, 10)), second=0`. Se mataron los procesos ARQ huérfanos (PID 24303 y 29281), se limpió el lock residual de Redis, se agregó test de regresión en `test_notifications_fase7.py` (950 passed) y se relanzó el worker ARQ (PID 30972, ~0.1% CPU). El job `notify_admin_approval` se ejecutó inmediatamente con `SUCCESS`, la notificación se entregó al admin Matias (`5844273174`) y se insertó el registro `notification_events` (`963fb298-cac5-4bd8-ab8d-19f3bb349ee7`, `delivery_status='delivered'`). Fuente: Redis CLI + ps/sample + Supabase MCP + pytest suite.
- 2026-08-13 — Auditoría de seguridad de BD cerrada. `auth_leaked_password_protection` es Pro-only (no aplicable en Free); mitigación aplicada por el usuario: "Allow new users to sign up" desactivado en Auth → Providers → Email. Verificado contra la API real: `POST /auth/v1/signup` con anon key → 422 `signup_disabled` ("Signups not allowed for this instance") y 0 usuarios creados. Fuente: curl + auth API + Supabase MCP.
- 2026-08-13 — Migraciones `20260813150000_sdd019_rpc_web_authz_guards.sql` y `20260813160000_sdd019_rpc_web_authz_guards_v2.sql` aplicadas (parity 56/56). Guards de autorización internos en las 2 RPCs expuestas a authenticated: `register_telegram_bot` y `seed_escritura_blocks` exigen `is_org_admin(p_org_id)` cuando `auth.role() = 'authenticated'` (y atribución `p_user_id = auth.uid()` en seed); service_role/postgres pasan. v1 usaba `coalesce(auth.role(),'') <> 'service_role'`, que bloqueaba SQL directo sin JWT; v2 usa el patrón canónico. Verificado con claims simuladas vía `set_config(request.jwt.claim.*)`: miembro no-admin → 42501 FORBIDDEN en ambas; admin → permitido; postgres directo → permitido. El smoke de admin sembró los 19 bloques de escritura de la org `7a0203ce-8b31-4661-a7b7-933613d49069` (idempotente, sin daño). Fuente: MCP + CLI linked.
- 2026-08-13 — Migración `20260813100000_sdd019_security_and_privilege_hardening.sql` aplicada al linked. (1) `REVOKE TRUNCATE, REFERENCES` en todas las tablas public de `anon`/`authenticated` + `ALTER DEFAULT PRIVILEGES` (TRUNCATE ignora RLS). (2) Lockdown de 36 funciones SECURITY DEFINER por categorías verificadas contra callers: Cat. 1 web (`register_telegram_bot`, `seed_escritura_blocks` → mantienen authenticated), Cat. 2 backend-only (approve/reject sale+reservation, create*\*\_request_db, activate_project_sales, add_mcp_connection, seed_default, assert_sdd_007, can*_, is*project_vendible, is_lot_operable → solo service_role/postgres), Cat. 3 helpers RLS (`is_org_user/admin/project_admin/project_vendor/super_admin` → mantienen authenticated, revocado anon+PUBLIC), Cat. 4 triggers y validate*_ (→ postgres/service_role). (3) `SET search_path = ''` en 3 funciones INVOKER. (4) `idle_in_transaction_session_timeout` por rol: anon 3s, authenticated 8s, authenticator 10s. (5) 2 índices duplicados eliminados (`idx_telegram_bots_org`, `escritura_template_clauses_template_idx`). (6) `dead_letter_queue.created_at/attempts` → NOT NULL. Verificación post-apply: advisors 0 WARN anon-definer/search_path (quedan 7 authenticated intencionales + leaked password protection pendiente en dashboard), smoke `SET ROLE anon` → permission denied; `SET ROLE authenticated` → helpers RLS operativos. M6 (leaked password protection) NO aplicable: proyecto en plan Free y la feature es Pro-only (doc oficial password-security). El único flujo con password del producto es login web (`LoginForm.tsx` signInWithPassword); no hay signup en web/API. Mitigación Free: desactivar "Allow new users to sign up" en Auth → Providers → Email y/o subir longitud mínima de password. Fuente: Supabase MCP + CLI linked + advisors + docs oficiales.
- 2026-08-13 — Plan de Estabilización de CPU COMPLETO y verificado en cloud. 8 migraciones SDD019 aplicadas en total (`20260812165003`/`20260812165010`/`20260812220304`/`20260812222206`/`20260812224500`/`20260812225000`/`20260813000001`/`20260813000002`). (1) Singleton + timeouts del pool (F1–F6). (2) RLS InitPlan **completado**: 14 instancias críticas de T-CPU-01 (`20260812220304`+`20260812222206`) + 28 residuales via `20260813000001_sdd019_rls_initplan_v2.sql` (auth/org/vendor/perfiles/agent_skills). Verificación: `pg_policies` reporta **0 políticas con `auth.uid()` o `auth.role()` sin wrapper**. (3) `20260812224500_sdd019_epoch_statement_triggers.sql` convirtió 5 triggers epoch a `FOR EACH STATEMENT` con transition tables; **fix PG17**: las transition tables no aceptan column lists (`UPDATE OF col1,col2`) — el filtrado se pasó al interior de las funciones via `JOIN new_table↔old_table` sobre la PK, preservando semántica original. 10 statement triggers activos verificados. (4) `20260812225000` reemplazó `digest(...,'sha256')` → `md5()` en `event_fingerprint` y `audit_event_key` de `approve_sale`; `create_sale_request_db` y `create_reservation_request_db` mantienen SHA-256 para `request_hash` (idempotency payload) por la decisión del plan. (5) `20260813000002_sdd019_fk_indexes_critical.sql` creó 19 índices en FKs críticas del workflow hot path (workflow_outbox, audit_logs, escritura_cascade/minuta/signature, legal_documents, vendor_membership_operations, etc.). (6) Monitor ARQ `check_idle_transactions_cron` (10 min, detection-only). (7) Runbook + evidencia actualizados. Estado runtime post-fix verificado: 0 idle_in_transaction, 13 conexiones totales, outbox 12/12 completed. Fuente: `supabase_migrations.schema_migrations` + `pg_trigger` + `pg_policies` + `pg_stat_activity` + `pg_indexes` vía Supabase MCP; suite pytest 949 passed / 1 fail pre-existente `test_trigger_exists_in_db`.
- 2026-08-12 — Agotamiento del pool PostgREST (PGRST003) diagnosticado: `get_supabase_client()` creaba un cliente httpx nuevo por llamada (~145 call sites) con timeout 120s; peticiones abandonadas dejaban transacciones huérfanas (`idle in transaction`) que Supavisor retiene para siempre (`idle_in_transaction_session_timeout=0`), retenían locks de fila sobre `workflow_outbox` y generaban más timeouts (loop) hasta agotar el pool. El puente operacional hacía supersede (UPDATE ok) + insert (INSERT falla) → variables `superseded` sin reemplazo ("faltantes aunque aprobadas"). Fixes F1–F6 aplicados: singleton cliente (timeout 30s, pool HTTP/1.1 10/5), `idle_in_transaction_session_timeout='5min'` (registrado en `pg_db_role_setting`), `lock_timeout=5s` en RPCs del outbox, script de monitoreo + runbook, test de regresión singleton. 7 zombies terminados manualmente. Fuente: inspección pg_stat_activity/pg_locks + docs Supabase (Context7) + verificación en suite.

- 2026-08-07 — Diagnóstico "venta aprobada sin escritura": el `approve_sale` remoto crea outbox (`workflow_outbox_id`), lo que desactiva el hook inline; el consumidor nunca completa porque `run_case_cascade` pasa `control=None` a `resolve_feature_rollout` (resulta `missing` → defiere con `AUTOMATIC_ESCRITURA_MISSING`, limpia el lease y rompe el `complete()` posterior) — trabajo ya cubierto por T079–T082. Además el caso del lote 47 queda `blocked` por gates `sii_verified` (`lote.rol_tramite`) y `legal_review_ready` (`revision_juridica.estado`). 5 ventas (lotes 47, 34, 2, 8, 16) en outbox `dead_letter`/`processing`. Evidencia: inspección BD cloud + reproducción local del hook/cascada. Fuente: verificación directa.
- 2026-08-07 — Divergencia de migraciones repo↔cloud: 5 remote-only (20260729023506, 20260729043333, 20260729043821, 20260729044941, 20260730012841) ausentes del repo y `20260806000000_sdd019_fix_outbox_and_vendible_rpc.sql` local sin aplicar al linked. Fuente: `supabase migration list --linked`.
- 2026-08-07 — En diagnóstico se ejecutó manualmente el hook para el lote 47: caso `93d9a7fc-a7d2-4364-ae9f-b9621b5cbc39` y borrador `02603b2c-6c2c-4433-ba26-a1a485aad382` creados (readiness `blocked`). Fuente: ejecución directa verificada.
- 2026-08-06 — La aprobación comercial de venta debe generar y despachar automáticamente el borrador de escritura (PDF/DOCX) por Telegram a Vendedor y Administrador sin bloqueos post-venta, utilizando la matriz aprobada previa y deslindes validados en visor. Fuente: usuario.
- 2026-08-06 — `plotify_memori/` es el vault de Obsidian y memoria profunda; `memory.md` no debe copiarlo. Fuente: usuario.
- 2026-08-06 — Rules y workflows heredados se retiran para eliminar instrucciones contradictorias. Fuente: usuario y auditoría de `.agents/`.

## Skills usadas y motivo

- 2026-08-18 — `telegram-mini-app`, `frontend-ui-engineering`, `ui-ux-pro-max`: Rediseño integral y profesional de la Telegram Mini App de Plotify. Construcción de Bottom Navigation Bar ergonómica, retroalimentación háptica nativa, centro de mando ejecutivo para administrador, pipeline comercial positivo para vendedor, selector de proyectos con contadores de inventario en tiempo real y formulario de reservas asistido.
- 2026-08-18 — `test-driven-development`: ciclo RED→GREEN de los 6 fixes de la miniapp de Telegram (proyectos endpoint, bot_username, contrato bandeja, deep link lot_, share, guard blockers) con pruebas de regresión en API y web.
- 2026-08-18 — `browser-testing-with-devtools`: consultada para el flujo de verificación en navegador (no ejecutada: sin MCP de Chrome disponible; verificación por tests y gates).
- 2026-08-18 — Context7 (next/script y manejo de nonce en head-manager/script.tsx): diagnóstico del hydration mismatch del SDK de Telegram en /mini y diseño del fix con script plano del server.
- 2026-08-18 — `test-driven-development` + `python-testing-patterns`: ciclo RED→GREEN del dedupe idempotente de notification_events y tests cross-tenant reales (RED confirmado con 2 filas duplicadas por `asyncio.gather`; GREEN tras upsert DO NOTHING + índice único; suite 984 passed).

- 2026-08-17 — `debugging-and-error-recovery`: diagnóstico y resolución mediante Sequential Thinking de 3 bugs encadenados en aprobación de venta, cascada de escritura, worker outbox y 500 en inventario de variables legales del proyecto.
- 2026-08-17 — `plotify-sdd-handoff`: cierre del SDD 021 tras revisión senior (handoff en vault, Home enlazado, notas de API/Workers actualizadas, memory sincronizada, check:agent-context y codegraph sync).
- 2026-08-14 — `speckit-plan` + `speckit-tasks` + `speckit-analyze` + `speckit-implement`: ciclo SDD completo de la feature 021 (plan.md, research.md, data-model.md, contracts/, quickstart.md, tasks.md 26 tareas, análisis sin hallazgos CRITICAL, implementación 26/26 con Verify).
- 2026-08-14 — `debugging-and-error-recovery`: aislamiento por capas (BD cloud → webhook/API → worker ARQ → Telegram) del flujo de notificaciones; evidencia en audit_logs/notification_events; identificó la doble ejecución inline+ARQ y el desajuste de roles.
- 2026-08-14 — `telegram-integration`: mapeo del flujo de decisión por callback Telegram (enqueue + camino corto inline) y del envío de mensajes al vendedor en send_decision_notifications.
- 2026-08-13 — `debugging-and-error-recovery`: aislamiento por capas (BD cloud, cascada, puente operacional, snapshot, notificaciones Telegram, worker ARQ) del bug endémico "falta aprobar" en venta Lote 26 Teno 2. Identificación de 4 bugs de persistenciaencadenados (snapshot stale, bifurcación fake/real, re-read matriz, state persistido incoherente) y diseño del hotfix con Verify pytest.
- 2026-08-13 — `supabase-postgres-best-practices`: auditoría integral de la BD cloud (tablas, índices, FKs, RLS, grants, triggers, ACLs de funciones) y diseño del lockdown de privilegios resultante (migración 20260813100000).
- 2026-08-13 — `telegram-integration`: diagnóstico del flujo de notificación Telegram, resolución del bot por organización y validación no mutante contra Bot API.
- 2026-08-13 — `debugging-and-error-recovery`: aislamiento por capas de la incidencia lote→BD→Redis→worker→Telegram; evidencia final en solicitud pendiente, job retenido y ausencia de worker.
- 2026-08-06 — `context-engineering`: diseñar jerarquía de contexto persistente.
- 2026-08-06 — `documentation-and-adrs`: estructurar documentación durable en Obsidian.
- 2026-08-06 — `skill-creator`: crear `plotify-sdd-handoff`.
- 2026-08-06 — `test-driven-development`: cubrir el verificador antes de implementarlo.
- 2026-08-06 — `git-workflow-and-versioning`: aislar el cambio documental de producto.

## Handoffs y bitácora

- Handoffs históricos: `plotify_memori/50 - Implementaciones/`.
- SDD019 sigue activo; no emitir recibo de cierre hasta completar gates y evidencia.
- 2026-08-06 — Se adoptó el modelo AGENTS para normas, memory para estado y `plotify_memori/` para documentación profunda.

## Protocolo de actualización

1. Registrar una corrección del usuario como pendiente, con fecha y fuente.
2. Contrastar con código, SDD, tests, CodeGraph o decisión explícita.
3. Promover sólo hechos confirmados y enlazar la evidencia.
4. Al cerrar un SDD, usar `$plotify-sdd-handoff` para actualizar vault, Home, handoff y memoria.
5. Ejecutar `pnpm check:agent-context` tras modificar AGENTS, memoria, mapa técnico o feature activa.
